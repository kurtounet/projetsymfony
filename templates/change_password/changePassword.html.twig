
{% extends 'base.html.twig' %}

{% block body %}
<div class="flex items-center justify-center p-4 ">
    <div class="w-1/2  bg-gray-300 rounded-lg p-4 shadow">
    <h1>Changement de mots de passe</h1>

    {{ form_start(changePasswordForm) }}
        {{ form_row(changePasswordForm.password) }}             
        {{ form_row(changePasswordForm.submit, {'attr': {'class': 'bg-blue-500 hover:bg-blue-700 text-white font-bold m-4 py-2 px-4 rounded'}}) }}     
    {{ form_end(changePasswordForm) }}
    
    </div>
    </div>
{% endblock %}

