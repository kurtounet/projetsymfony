<header class="bg-gray-800 p-4 text-white flex justify-between items-center">
	<div class="text-lg font-bold">DBZ</div>
	<nav>
		<ul class="flex gap-4">
			<li>
				<a href="{{ path('app_index') }}" class="hover:text-gray-300">Accueil</a>
			</li>
			<li>
				<a href="{{ path('app_heros') }}" class="hover:text-gray-300">Heros</a>
			</li>
			<li>
				<a href="{{ path('app_about') }}" class="hover:text-gray-300">À propos</a>
			</li>
			<li>
				<a href="{{ path('app_contact') }}" class="hover:text-gray-300">Contact</a>
			</li>
			<li>
				<a href="{{ path('app_newsletter_subscribe') }}" class="hover:text-gray-300">Newsletter</a>
			</li>
		</ul>
	</nav>
	<div>
		<a class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded" href="{{ path('app_login') }}">

			Se connecter
		</a>
		{% if ( is_granted('ROLE_ADMIN') or is_granted('ROLE_ADMIN') )%}
		<a class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded" href="{{ path('app_logout') }}">

			Se déconnecter
		</a>
		{% endif %}
	</div>
</header>
