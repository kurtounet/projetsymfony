{% extends 'base.html.twig' %}

{% block title %}Contact !{% endblock %}

{% block body %}
<section class="bg-gray-300 rounded-lg w-1/2 m-5 p-4 sm:w-1/2 lg:w-1/3 mx-auto"> 
    <h2>Nous Contacter</h2>
        <div class="m-2" >
        {# {{ form(Contact_form) }} #}
        {# form_template.html.twig #}
    
        {{ form_start(Contact_form) }}
        
            {{ form_row(Contact_form.firstName) }}
            {{ form_row(Contact_form.lastName) }}
            {{ form_row(Contact_form.subject) }}
            {{ form_row(Contact_form.email) }}
            {{ form_row(Contact_form.message) }}      
           
        {{ form_end(Contact_form) }}
        </div>
</section>
{% endblock %}
